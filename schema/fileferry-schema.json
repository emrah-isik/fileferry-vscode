{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FileFerry Configuration",
  "description": "Per-project deployment bindings for the FileFerry VSCode extension",
  "type": "object",
  "properties": {
    "defaultServerId": {
      "type": "string",
      "description": "ID of the default server used for Alt+U uploads"
    },
    "uploadOnSave": {
      "type": "boolean",
      "description": "When true, files are automatically uploaded to the default server on save"
    },
    "servers": {
      "type": "object",
      "description": "Per-server bindings keyed by server ID",
      "additionalProperties": {
        "type": "object",
        "required": ["mappings", "excludedPaths"],
        "properties": {
          "mappings": {
            "type": "array",
            "description": "Local-to-remote path mappings. More specific paths take priority.",
            "items": {
              "type": "object",
              "required": ["localPath", "remotePath"],
              "properties": {
                "localPath": {
                  "type": "string",
                  "description": "Local path relative to workspace root. Use '/' for the whole project."
                },
                "remotePath": {
                  "type": "string",
                  "description": "Absolute path on the remote server."
                }
              },
              "additionalProperties": false
            },
            "minItems": 1
          },
          "excludedPaths": {
            "type": "array",
            "description": "Glob patterns for files/folders to never upload (e.g. 'node_modules', '*.log', '.env')",
            "items": {
              "type": "string"
            }
          },
          "rootPathOverride": {
            "type": "string",
            "description": "Overrides the server's root path for this project only"
          }
        },
        "additionalProperties": false
      }
    }
  },
  "required": ["defaultServerId", "servers"],
  "additionalProperties": false
}
